import { BigNumber } from "bignumber.js"

export class StatHandler {
    public static getComputedValue(stat: string, base: BigNumber): BigNumber {
        return BigNumber(1);
    }

    public static getStatTotal(stat: string): BigNumber {
        return BigNumber(1);
    }
}
